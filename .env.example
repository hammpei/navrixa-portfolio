# Set your final production origin when deploying to Vercel or a custom domain.
NEXT_PUBLIC_SITE_URL=https://navrixa-hammad.hammapei.chatgpt.site
# Optional: endpoint accepting JSON {name,email,company,projectType,message}.
# Without an endpoint, the form prepares a mailto draft and never claims to send it.
NEXT_PUBLIC_CONTACT_ENDPOINT=
